#pragma once

#include <string>
#include <filesystem>

#include <windows.h>

namespace alpha1 {
    struct mod_info {
        std::string name;
        std::string version;
        std::string author;
    };
}